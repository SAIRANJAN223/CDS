projection;
strict ( 1 );
use draft;

define behavior for ZC_TRAVEL_SAI alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action deductDiscount;
  use action copyTravel;

  use action acceptTravel;
  use action rejectTravel;


}